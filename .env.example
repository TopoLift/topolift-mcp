# Get a Bearer key at https://topolift.ai (or pay per call via x402 directly).
TOPOLIFT_API_KEY=

# Override only if you're pointing at a non-default deployment:
# TOPOLIFT_API_URL=https://api.topolift.ai
# TOPOLIFT_TIMEOUT=600
# TOPOLIFT_LOG_LEVEL=INFO
